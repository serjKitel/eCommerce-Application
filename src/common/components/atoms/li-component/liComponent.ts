import { createElement } from '../../../utils/createElement';
import { TAGS } from '../../../constants/tags';

export const liElement = createElement({
  tag: TAGS.li,
  className: 'navigation__item',
});
